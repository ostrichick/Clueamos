# 📜 Clueamos 프로젝트 상세 컨텍스트 (PROJECT_CONTEXT.md)

> **문서 역할:** AI 인수인계용 기준 문서 (기획·룰·기술 원칙).
> 이 문서는 **ChatGPT, Claude, Gemini 등 서로 다른 AI 에이전트들이 번갈아 작업할 때 프로젝트의 맥락과 방향성을 잃지 않도록 돕는 기준 문서**입니다. 새로운 대화 세션을 시작할 때 이 파일을 가장 먼저 읽도록 안내하세요.

---

## 1. 기획 배경 및 의도
* **목적:** 사용자가 아내와 함께 2인으로 플레이할 수 있는 추리 보드게임 웹앱 제작.
* **해결하고자 하는 문제:** 2인이 Clue를 하면 금방 정보가 소진되고 뻔해짐. 따라서 **컴퓨터 AI 탐정 2명을 추가하여 4인 구도**를 형성함.
* **플레이 형태:**
  * 기본 MVP: 한 컴퓨터/태블릿 브라우저에서 번갈아가며 화면을 보는 패스 앤 플레이(Pass-and-Play) 형태 또는 개인 손패 가림 UI 제공.
  * 추후: 로컬 멀티플레이 / 방 코드 형태 확장 고려.

---

## 2. 게임 디자인 및 룰 요약

### (1) 정답(Solution)의 3요소 (정통 Clue)
* **용의자 (Suspect):** 6명
* **장소 (Location):** 6곳
* **도구 (Weapon):** 8개 *(리드파이프·황금 트로피 포함)*
* 총 20장 카드 체계, 정답 봉투 3장은 비밀 격리

### (2) 플레이어 구성 (2인 인간 + AI 탐정)
* 기본 4인 구도: **Player 1 (인간) + Player 2 (인간) + AI 아서 + AI 블레이크**
* 플레이 모드에 따라 인간 수가 달라짐: **솔로(1:1 vs AI)** / **로컬(2인 패스앤플레이)** / **온라인(호스트-게스트)**
* AI 탐정 수는 1~5명까지 커스터마이즈 가능
* **AI 아서 (논리형):** 얻은 단서를 정밀하게 소거하고 확률을 계산함. 확신도 100%에 도달할 때만 고발.
* **AI 블레이크 (직감형):** 특정 용의자/장소를 집요하게 추궁하거나, 때로는 블러핑(자신이 가진 카드로 질문)을 시도. 확신도가 낮아도 과감하게 승부수를 던짐.

> ⚠️ **핵심 원칙:** AI는 절대로 숨겨진 정답(Secret Solution)을 치팅하여 알고 있어서는 안 됩니다. 인간 플레이어와 똑같이 자신이 받은 카드와 라운드 중 오고 간 질문/답변 기록만을 바탕으로 판단해야 합니다.

### (3) 턴 흐름 (Turn Loop)
1. **이동/조사:** 보드 내 장소로 이동 (주사위 2개 굴림 또는 비밀 통로).
2. **질문 (Suggestion):** 현재 위치한 장소 + 용의자 + 도구 조합으로 다른 탐정들에게 질의.
3. **반증 (Disprove):** 질문받은 다음 플레이어부터 시계방향으로 질문된 카드 중 하나라도 소지하고 있다면 비밀리에 1장을 제시하여 반증 (없으면 패스).
4. **추리 노트 갱신 (Deduction Note):** 반증 결과 및 질의 히스토리가 자동으로 기록됨.
5. **행동 완료 / 턴 종료:** 행동 후 턴 종료 또는 최종 고발 중 선택.
6. **최종 고발 (Accusation):** 턴 중 언제든 선언 가능. 맞으면 승리, 틀리면 탈락. 최대 16턴 제한.
7. **AFK 대리 플레이:** 인간 플레이어가 장시간 자리 비우면(90초) AI가 자동으로 턴을 대리 진행.

---

## 3. 기술적 원칙 및 코딩 규칙
* **엔진-UI 철저 분리:**
  * 모든 게임 규칙과 상태 변경은 `src/engine/` 아래의 순수 TypeScript 모듈로 작성할 것.
  * 상태 변경은 불변성(Immutability)을 유지하며 리듀서 또는 순수 함수 형태로 작성하여 테스트 코드(`Vitest` / `Jest`) 작성이 용이하도록 할 것.
* **AI 인터페이스 규격화:**
  ```typescript
  export interface AIAgent {
    id: string;
    name: string;
    personality: 'LOGICAL' | 'INSTINCTIVE';
    decideAction: (state: ReadonlyGameState, playerId: string) => PlayerAction;
  }
  ```
  AI는 순수하게 `GameState`를 읽고 `Action` 객체만 반환하도록 설계.
* **반응형 UI & 감성적 디자인:**
  * 부부가 아늑하게 즐길 수 있는 미스터리 호텔 무드 (다크 모드, 엔틱 골드, 크림 텍스처, 가독성 높은 폰트).
  * 자동 추리 노트는 체크/X/물음표를 쉽게 토글할 수 있는 모던 인터페이스.

---

## 4. 인수인계 시 주의사항 (다른 AI에게)
* 기존 코드를 불필요하게 전부 재작성(Refactoring)하지 마세요.
* 변경 전에는 반드시 `PROGRESS.md`의 [진행 현황]과 [다음 할 일]을 확인하세요.
* 작업이 끝나면 반드시 `PROGRESS.md`에 본인이 한 작업과 다음 세션에서 할 일을 갱신하세요.
